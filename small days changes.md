# Small Days Changes

Completed:

- Removed the header `#top` anchor behavior so clicking the brand no longer changes the URL.
- Updated the header subtitle to `Crafted in the veld, built in the open.`
- Removed the footer links that duplicated the main release buttons.
- Moved shared page shell styles into `src/styles.css` so the Angular component-style budget warning is cleared.
- Removed the duplicate root `CNAME` source in favor of `public/CNAME`.
- Removed the unused `release.description` field from `src/app/data/site-content.ts`.
- Expanded the page title for clearer browser tabs and previews.
- Switched the background image path to the same built asset style used elsewhere in the app.
